<% @votings.each do |voting|%>
	<%= voting %></br>
	<%= voting.user_id %></br>
	<%= link_to 'show', voting_path(voting) %></br>
	<% voting.opinions.each do |opinion| %>
		<%= opinion.description %></br>
	<% end %>
<% end %>
<%= link_to 'New', new_voting_path %>
